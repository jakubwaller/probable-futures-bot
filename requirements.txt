python-telegram-bot==13.11
pandas==2.2.3
requests==2.32.5
# ptb 13's vendored urllib3 breaks on python 3.12; it falls back to the system
# urllib3, which must be <2 (v2 dropped contrib.appengine that ptb imports)
urllib3==1.26.20
